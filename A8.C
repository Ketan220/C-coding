/*Program tp search an element in an array*/
main()
{
     int arr[5]={1,2,3,4,5};
     int i,num,
     clrscr();
     printf("Enter the number");
     scanf("%d",&num);
     for(i=1;i<5;i++)
     {
	  if(num=arr[5])
	  {
	     printf("No. is found=%d",num);
	     break;
	  }
     }
     if(i=5)
     {
     printf("No. is not found");
     }
     getch();
}

