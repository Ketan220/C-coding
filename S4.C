/* Reads & writes string in c using gets and puts function*/
#include<string.h>
main()
{
     char name[20];
     clrscr();
     puts("Enter you name");
     gets(name);
     puts("name is"  );
     puts(name);
     getch();
}
